export type PageNumberFieldFormat = {
    format?: 'decimal' | 'upperRoman' | 'lowerRoman' | 'upperLetter' | 'lowerLetter' | 'numberInDash' | 'ordinal' | 'hebrew1' | 'hebrew2';
    zeroPadding?: number;
    numericPicture?: string;
};
export type PageNumberFormat = NonNullable<PageNumberFieldFormat['format']>;
export type PageNumberChapterSeparator = 'hyphen' | 'period' | 'colon' | 'emDash' | 'enDash';
export declare function formatPageNumber(pageNumber: number, format: PageNumberFormat): string;
export declare function formatPageNumberFieldValue(pageNumber: number, fieldFormat?: PageNumberFieldFormat): string;
export declare function formatChapterPageNumberText(args: {
    pageComponent: string;
    chapterNumberText?: string;
    chapterSeparator?: PageNumberChapterSeparator;
}): string;
export declare function formatSectionPageNumberText(args: {
    displayNumber: number;
    pageFormat: PageNumberFormat;
    chapterNumberText?: string;
    chapterSeparator?: PageNumberChapterSeparator;
}): string;
/**
 * Formats integer page field values with a Word numeric picture subset.
 * Unsupported ECMA features are intentionally out of scope here: backtick
 * numbered-item references, localized separators, and fractional rounding.
 */
export declare function formatIntegerWithNumericPicture(value: number, picture: string): string;
