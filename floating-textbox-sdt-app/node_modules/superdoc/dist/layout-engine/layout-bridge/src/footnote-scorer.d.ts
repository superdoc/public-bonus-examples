import { FootnotePageLedger, Layout } from '../../contracts/src/index.js';
export type FootnoteWindowScoreReason = 'globally-safe' | 'cluster-spill' | 'new-mandatory-only' | 'candidate-not-improved' | 'page-count-grew' | 'dead-reserve-bloat';
export type FootnotePreferredReserveCandidate = {
    pageIndex: number;
    anchorIds: string[];
    mandatoryReservePx: number;
    preferredReservePx: number;
    reserveDeltaPx: number;
    actualBandHeightPx: number;
    lastAnchorRenderedLines: number;
};
export type FootnoteWindowStats = {
    totalPages: number;
    mandatoryOnlyCount: number;
    deadReserveSum: number;
    clusterSplitCount: number;
    candidateRenderedLines?: number;
    /**
     * Analyzer-only metric. Runtime layout does not know the Word baseline, but
     * the scorer can carry this when a caller has external alignment data.
     */
    driftEvents?: number;
};
export type FootnoteWindowScoreInput = {
    beforeLayout: Layout;
    afterLayout: Layout;
    candidatePageIndex: number;
    candidateAnchorId?: string;
    beforeLedger: FootnotePageLedger[];
    afterLedger: FootnotePageLedger[];
    windowAhead?: number;
    preferredDeltaThresholdPx?: number;
    mandatoryOnlyTolerancePx?: number;
    deadReserveBloatThresholdPx?: number;
    wholeDocumentDeadReserveBloatThresholdPx?: number;
};
export type FootnoteWindowScoreResult = {
    accept: boolean;
    reason: FootnoteWindowScoreReason;
    before: FootnoteWindowStats;
    after: FootnoteWindowStats;
};
export declare const isMandatoryOnlyFootnotePage: (ledger: FootnotePageLedger, preferredDeltaThresholdPx?: number, mandatoryOnlyTolerancePx?: number) => boolean;
/**
 * SD-2656: a page whose LAST anchor partially rendered
 * but spilled to a later page. The user-visible bug is a footnote split across
 * pages even when the preferred reserve would fit the whole anchor on the
 * anchor page (Word does keep it together).
 *
 * The "mandatory-only" predicate catches first-line-only splits; this predicate
 * catches partial splits (lastAnchorRenderedLines > 1 but the rest still spilled).
 * Both feed into the same scorer trial. The scorer's accept criteria
 * (no new cluster spills, no new mandatory-only pages, bounded dead-reserve
 * growth, candidate rendered lines improved) still gates whether the bump
 * actually lands.
 */
export declare const isSplitLastAnchorFootnotePage: (ledger: FootnotePageLedger, preferredDeltaThresholdPx?: number) => boolean;
export declare const getPreferredReserveCandidates: (ledgers: FootnotePageLedger[], preferredDeltaThresholdPx?: number, mandatoryOnlyTolerancePx?: number) => FootnotePreferredReserveCandidate[];
export declare const getPreferredReserveTrialTargets: (candidate: FootnotePreferredReserveCandidate, currentReservePx: number, preferredDeltaThresholdPx?: number, maxTargets?: number) => number[];
/**
 * A single remaining range cannot increase the candidate's rendered-line
 * count unless the trial reserve can fit that whole range after the content
 * already in the band. Multi-range continuations stay speculative because
 * the ledger does not expose the first remaining range's individual height.
 */
export declare const canPreferredReserveTargetImproveCandidate: (candidate: FootnotePreferredReserveCandidate, continuation: FootnotePageLedger["continuationOut"][number] | undefined, targetReservePx: number, epsilon?: number) => boolean;
export declare const shouldAbsorbOneLineFootnoteWidow: (ledger: FootnotePageLedger, tailPx: number, oneLineTailPx?: number) => boolean;
export declare const collectFootnoteLedgers: (layout: Layout) => FootnotePageLedger[];
export declare const shouldRunPreferredReserveTrials: (layoutMode: "full" | "prefix-resume" | "tail-splice", pagesPaginated: number | null, maxWarmRelaidPages: number) => boolean;
export declare const summarizeFootnoteWindow: (layout: Layout, ledgers: FootnotePageLedger[], candidatePageIndex: number, windowAhead?: number, preferredDeltaThresholdPx?: number, mandatoryOnlyTolerancePx?: number, candidateAnchorId?: string) => FootnoteWindowStats;
export declare const scoreFootnoteWindow: (input: FootnoteWindowScoreInput) => FootnoteWindowScoreResult;
